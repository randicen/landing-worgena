# Vacío por ahora. Las reglas de redirección se agregan cuando se configure
# el dominio custom (worgena.com). Formato esperado por Cloudflare Pages:
#
#   /ruta-origen  /ruta-destino  301
#
# Ver historial de git para las reglas originales (commit previo al fix).